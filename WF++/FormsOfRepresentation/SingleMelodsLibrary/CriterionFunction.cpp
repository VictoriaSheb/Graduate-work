#include "CriterionFunction.h"


CriterionFunction::CriterionFunction(Bitmap fii, Bitmap fti) 
{
   // Fii = WorkWithImages.GetBits(new Bitmap(fii));
   // Fti = WorkWithImages.GetBits(new Bitmap(fti));
   // K = Fti.GetLength(1);
  /*  L = Fti.GetLength(0);
    N = Fii.GetLength(1);
    M = Fii.GetLength(0);
    Height = L - M;
    Width = K - N;
    Fcriterion = new double[Height, Width];
    Count = 0;*/
}